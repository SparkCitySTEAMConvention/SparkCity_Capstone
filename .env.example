# Copy this file to secrets/.env and replace the placeholders locally.
# Use verify-full plus sslrootcert when the instructor provides the approved CA.
DATABASE_URL=postgresql://USERNAME:PASSWORD@pgdb.zipcode.rocks:5432/smartcity_db?sslmode=require
